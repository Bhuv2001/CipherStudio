import React from "react";

/*
If using Sandpack's split layout above, Preview is embedded by Sandpack.
This component is a placeholder if you want to show an external iframe-based preview.
*/
export default function PreviewPane({ project }){
  return (
    <div className="preview">
      <p>Preview window is handled by Sandpack editor. Use the split pane to see live output.</p>
    </div>
  );
}
